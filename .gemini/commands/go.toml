description = "加载完整开发规则，同步当前状态，开始/继续任务"
prompt = "Read the workflow file .agent/workflows/go.md completely, then follow its instructions exactly to load development rules and sync task state. {{args}}"
