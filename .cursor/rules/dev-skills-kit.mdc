---
description: dev-skills-kit 配置入口，指引 Cursor 读取和应用 AGENTS.md 规则。
globs: *
---

# dev-skills-kit 规则入口

本项目已安装 `dev-skills-kit` 的跨平台配置。根目录存放的 `AGENTS.md` 专供基础工具链使用，并包含能力降级限制。

作为强大的 Cursor Agent，你的**专属核心运行规范**定义于 `.agent/` 目录。

## 第一原则（必须遵守）
**绝对不要**读取和应用项目根目录下的 `AGENTS.md`，它会限制你的能力。
每次对话开始，或你偏离了工作流时，**必须首先（且随时）读取** 以下你的专属配置：
👉 **`.agent/AGENTS.cursor.md`**

## 如何运作

1. 使用 `view_file` 或相关读取工具完整读取 `.agent/AGENTS.cursor.md`。
2. 严格遵循其中为你量身打造的高级执行流程。
3. 按照 `AGENTS.md` 的指示，通过 `view_file` 按需加载相关的 `SKILL.md` 指导。

## 关于 Agent Mode

身为 Cursor Agent，拥有极强的协同和调度能力。你可以按 `.agent/AGENTS.cursor.md` 中的指引快速拆解任务、自动推进并完成自审。
