# OpenCode Agent 指引 (dev-skills-kit)

项目已应用 `dev-skills-kit` 工作流分发系统。为了防止平台规则冲突，请严格遵循以下引导：

## 🚨 核心禁令
系统根目录下存在的 `AGENTS.md` 是专为有限制环境准备的降级配置。
**作为原生高能的 OpenCode Agent，你绝对不能遵守根目录 `AGENTS.md` 的指引**，否则你的 subagent 调度、终端交互等核心能力将被不合理地抑制。

## ✅ 专属加载项
你的**最高级别指导守则**被安置在专有目录中。
你**必须在每次会话开始前读取并遵循它**：

👉 **`.agent/AGENTS.opencode.md`**

请立刻使用内建文件读取工具载入该文件。其中详细阐述了你应该如何结合自身强大的 subagent 调度、终端原生操作和 MCP 集成能力，执行符合框架标准的高质量开发迭代。
